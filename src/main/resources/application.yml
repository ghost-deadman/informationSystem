spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/information_system?useUnicode=true&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&useSSL=false
    username: root
    password: 8471426jkl

mybatis:
  #定义别名包
  type-aliases-package: com.example.informationsystem.pojo
  #导入映射文件(指定映射文件的路径)
  mapper-locations: classpath:/Mappers/*.xml
  #开启驼峰映射
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  #打印日志
  logging:
    level:
      com.example.informationsystem.mapper: debug
