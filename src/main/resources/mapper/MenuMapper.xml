<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.informationSystem.mapper.MenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.informationSystem.entity.Menu">
        <id column="id" property="id" />
        <result column="url" property="url" />
        <result column="path" property="path" />
        <result column="component" property="component" />
        <result column="menu_name" property="menuName" />
        <result column="icon" property="icon" />
        <result column="keepAlive" property="keepAlive" />
        <result column="requireAuth" property="requireAuth" />
        <result column="enabled" property="enabled" />
    </resultMap>


    <resultMap id="MenusMapWithRole" type="com.example.informationSystem.entity.Menu" extends="BaseResultMap">
        <collection property="roles" ofType="com.example.informationSystem.entity.Role">
            <id column="id" property="id" />
            <result column="name" property="name" />
            <result column="role_key" property="roleKey" />
        </collection>
    </resultMap>


    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, url, path, component, menu_name, icon, keepAlive, requireAuth, enabled
    </sql>
    <!--通过用户ID查询菜单列表-->
    <select id="getMenusByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT
            m1.*
        FROM
            sys_menu m1,
            sys_user_role ur,
            sys_role_menu mr
        WHERE
            m1.id = mr.menu_id
          AND mr.role_id = ur.role_id
          AND ur.user_id = #{id}
          AND m1.enabled = TRUE
        ORDER BY m1.id
    </select>
    <!--根据角色获取菜单列表-->
    <select id="getMenusByRole" resultMap="MenusMapWithRole">
        SELECT
            m.*,
            r.id AS rid,
            r.name AS name,
            r.role_key AS role_key
        FROM
            sys_menu m,
            sys_role r,
            sys_role_menu mr
        WHERE
            m.id = mr.menu_id
          AND
            r.id = mr.role_id
          AND
            r.enabled = TRUE
          AND
            m.enabled = TRUE
        ORDER BY m.id
    </select>
    <!--查询所有菜单-->
    <select id="queryAllMenus" resultMap="BaseResultMap">
        SELECT
            m.id AS id,
            m.menu_name AS name
        FROM
            sys_menu m
        WHERE
            m.enabled = TRUE
    </select>

</mapper>
