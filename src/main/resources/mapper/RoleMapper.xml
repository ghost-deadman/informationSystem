<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.informationSystem.mapper.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.informationSystem.entity.Role">
        <id column="id" property="id" />
        <result column="name" property="name"/>
        <result column="role_key" property="role_key"/>
        <result column="enabled" property="enabled"/>
        <result column="del_flag" property="del_flag"/>
        <result column="remark" property="remark"/>

    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, role_key, enabled, del_flag, remark
    </sql>

<!--    根据用户id获取用户角色-->
    <select id="getRoles" resultType="com.example.informationSystem.entity.Role">
        SELECT
            r.id,
            r.name,
            r.role_key
        FROM
            sys_role r,
            sys_user_role ur
        WHERE
            r.enabled = TRUE
          AND ur.role_id = r.id
          AND ur.user_id = #{id}
    </select>

</mapper>
